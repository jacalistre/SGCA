{% extends 'base.html.twig' %}

{% block title %}Notificacion{% endblock %}

{% block body %}
    <h1>Notificacion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ notificacion.id }}</td>
            </tr>
            <tr>
                <th>Mensaje</th>
                <td>{{ notificacion.mensaje }}</td>
            </tr>
            <tr>
                <th>Fecha_envio</th>
                <td>{{ notificacion.fechaEnvio ? notificacion.fechaEnvio|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Fecha_leido</th>
                <td>{{ notificacion.fechaLeido ? notificacion.fechaLeido|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Destino</th>
                <td>{{ notificacion.destino }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('notificacion_index') }}">back to list</a>

    <a href="{{ path('notificacion_edit', {'id': notificacion.id}) }}">edit</a>

    {{ include('notificacion/_delete_form.html.twig') }}
{% endblock %}
