<form method="post" action="{{ path('notificacion_delete', {'id': notificacion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ notificacion.id) }}">
    <button class="btn">Delete</button>
</form>
