{% extends 'base.html.twig' %}

{% block title %}Sala{% endblock %}

{% block body %}
    <h1>Sala</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sala.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ sala.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ sala.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sala_index') }}">back to list</a>

    <a href="{{ path('sala_edit', {'id': sala.id}) }}">edit</a>

    {{ include('sala/_delete_form.html.twig') }}
{% endblock %}
