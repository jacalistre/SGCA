<form method="post" action="{{ path('configuracion_delete', {'id': configuracion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ configuracion.id) }}">
    <button class="btn">Delete</button>
</form>
