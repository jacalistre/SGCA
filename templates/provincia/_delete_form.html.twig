<form method="post" action="{{ path('provincia_delete', {'id': provincium.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ provincium.id) }}">
    <button class="btn">Delete</button>
</form>
