{% extends 'base.html.twig' %}

{% block title %}EstadoCama{% endblock %}

{% block body %}
    <h1>EstadoCama</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ estado_cama.id }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ estado_cama.estado }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ estado_cama.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('estado_cama_index') }}">back to list</a>

    <a href="{{ path('estado_cama_edit', {'id': estado_cama.id}) }}">edit</a>

    {{ include('estado_cama/_delete_form.html.twig') }}
{% endblock %}
